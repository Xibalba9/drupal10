hello_world.hello_controller_hello:
  path: "/hello-world/{name}"
  defaults:
    name: World
    _title: "Hello, world!"
    _controller: '\Drupal\hello_world\Controller\HelloController::build'
  requirements:
    name: "[a-zA-Z]+"
    _permission: "access content"
